#include <stdio.h>
#include <stdlib.h>

/* int main()
{
    int a, b, c, max;
    printf("Enter three integers: ");
    scanf("%d %d %d", &a, &b, &c);

    max = a + b;
    if(b + c > max) {
        max = b + c;
    }
    if(c + a > max) {
        max = c + a;
    }
    printf("The maximum sum among a+b, b+c, and c+a is: %d\n", max);
    return 0;
} */

int main()
{
    int a, b, c;

}
